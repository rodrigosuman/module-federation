{
  "name": "module-federation",
  "private": true,
  "workspaces": {
    "packages": [
      "app1",
      "app2",
      "shell",
      "store"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run --parallel build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
